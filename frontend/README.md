# Frontend

This folder contains the React + Vite frontend for the Carbon Footprint Week 1–2 features.

Run locally:

1. cd frontend
2. npm install
3. npm run dev

The frontend expects the backend to run at http://localhost:8080 (proxy configured in vite.config.js).
